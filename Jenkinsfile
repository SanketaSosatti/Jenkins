pipeline {
agent any
stages {
      stage ('Bbuild')
      {
      steps {
        sh 'echo "hello World"'
        sh '''
          echo "Multiline shell steps works too"
          ls -lah
         
         '''
        }
    }
   }
   }
